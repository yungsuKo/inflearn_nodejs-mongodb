과거
    server <> client

    server 
        single codebase <> database

    client 
        url
        html, css, javascript



현재
    server(frontend) <> client
    server(backend) <> client
    server(database) <> client

    server

    client

    frontend에 해당하는 화면을 그림
    그 이후부터는 데이터와 연산만 진행하는 추게